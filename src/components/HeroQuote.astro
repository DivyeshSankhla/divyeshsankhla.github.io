---
interface Props {
  text: string;
  author: string;
}

const { text, author } = Astro.props;
---

<blockquote class="quote">
  <p class="quote__text">"{text}"</p>
  <p class="quote__attribution">{author}</p>
</blockquote>
